---
# Frontmatter (Metadaten)

# Entspricht dem Feld "title" in Ihrer Tina-Konfiguration
title: Die offizielle Homepage von LamaDrama

# Entspricht dem Feld "posted" (oder "date") in Ihrer Tina-Konfiguration
# WICHTIG: Verwenden Sie hier das exakte Datumsformat (ISO 8601), das Tina erwartet.
posted: 2025-10-02T18:50:00.000Z

# Falls Sie ein Feld "author" definiert haben:
author: Das Lama-Team

---

# Willkommen auf der Startseite!

Dies ist der **Hauptinhalt (Body)** Ihrer Seite.

Er wird von TinaCMS als **`rich-text`** interpretiert und ermöglicht Ihnen, normalen Markdown-Text zu verwenden.

## Die Vorteile von MDX

Da dies eine `.mdx`-Datei ist, können Sie hier direkt **React-Komponenten** einfügen. Das ist perfekt für Callouts, Hero-Sektionen oder interaktive Elemente.

<CustomButton link="/ueber-uns" label="Mehr über uns" />

Der Build-Prozess sollte diese Datei nun korrekt parsen, da der **Frontmatter-Block** (die Metadaten zwischen den `---`) und der **Hauptinhalt** (der Body) dem TinaCMS-Schema entsprechen.

**Achten Sie auf die Komponenten:** Damit `<CustomButton>` oder andere eigene Komponenten funktionieren, müssen diese in dem Next.js-Code bekannt sein, der den MDX-Inhalt rendert (wie in den Regeln beschrieben).
